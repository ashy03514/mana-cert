# V2 第六階段：音效、畫質與 PWA 收尾

## 共鳴完成卡住修正

快取版本更新為 `mana-pwa-v2-stage6-2`。修正 ActiveTimeline 將原生 setTimeout / clearTimeout 當類別方法呼叫的接收者錯誤，避免瀏覽器 Illegal invocation 阻止共鳴後的收束排程。新增原生呼叫限制的回歸測試，舊程式會失敗、修正後通過；完整頁面測試也加入同一限制。尚未代替使用者實機驗證。

## 已完成

- `js/audio.js` 提供 touch、movement、resonance、gather、element、rankUp、confirmed、ssWarning 音效槽。預設為低音量 Web Audio 合成音，不依賴外部檔案；可透過 `setBuffer(slot, decodedAudioBuffer)` 替換正式素材。
- 設定頁新增音效與震動開關。開始鑑定及首次觸碰時嘗試解鎖 AudioContext，支援 webkit 前綴。不支援、拒絕播放或震動拋錯時安全略過，不影響流程。
- 音效最多同時四個聲部，同一槽節流。重試、關閉音效、背景暫停會停止及釋放目前聲部。
- `muggleFailure` 僅預留音效槽，沒有呼叫；麻瓜專屬演出仍暫緩。既有通用共鳴、收束及定案回饋適用各輪。
- `js/runtime.js` 提供共用有效時間。切到背景時共鳴、視覺進度、序列計時器與跑分一起暫停；返回後繼續，不因離開時間跳過揭曉。背景不繪製或模擬粒子，原 RAF 排程仍由瀏覽器管理。
- 畫質根據 viewport 初選，持續慢幀後逐階降低 HIGH / NORMAL / LOW，穩定快幀後恢復。DPR 上限分別為 1.75 / 1.35 / 1；降低背景柔光、絲帶層與微粒繪製量。保留全部物理物件，不改動力場、結果或共鳴規則。
- 畫質調整有冷卻時間，忽略超長背景間隔，避免單次卡頓立刻切換。這是基礎自適應，非 iPhone 已實测達標的保證。
- `js/pwa.js` 在設定頁顯示離線資源狀態，提供「檢查更新」。新版下載後提示關閉同站分頁及 PWA 再開啟，不強制接管正在鑑定的頁面。

## 驗證

`node tests/stage6.cjs` 驗證背景計時暫停／恢復／取消、畫質降級與恢復、缺失與失敗音效 API、音效節流與聲部清除、可選震動、Service Worker 安裝快取、快取讀取及清除舊版範圍。

第一階段頁面回歸另外驗證背景停留 10 秒後，屬性揭曉與跑分仍留在原階段；其餘第二至第五階段測試保留執行。

未啟動瀏覽器或實機測試。實際 iPhone 聲音解鎖、音量、靜音模式、幀率、離線啟動與更新流程仍需使用者確認。

## 使用者實機驗收

1. 開啟音效，開始一輪：觸摸、滑動、共鳴、收束、屬性與跨階有短促回饋；音量是否適合展場之後再調。
2. 關閉音效：整輪無聲但流程正常。iPhone 沒有震動也應完全正常。
3. 操控、屬性揭曉、跑分途中各切到背景，等幾秒再回來；應從原進度接續，沒有越過結果揭曉。
4. 連續鑑定 10–20 輪，確認沒有音效疊加、粒子殘留、明顯逐輪變慢。
5. 連網開啟部署網站，等待「離線資源已就緒」後關閉，斷網再從主畫面開啟，確認完整鑑定可用。
6. 新版本部署後點「檢查更新」。若顯示已下載，關閉此網站所有分頁與 PWA 再開啟，確認新版本載入。

## 部署

快取版本：`mana-pwa-v2-stage6-1`。上傳 `index.html`、整個 `js/`、`service-worker.js`、`manifest.webmanifest`、兩張原圖示；不用 npm 或 build。manifest 保留既有相對 start_url、standalone 與圖示，無新增素材需要修改。

不需部署 tests、backups、階段文件與 V1 備份。任何已快取檔案有變更，都需遞增 Service Worker 快取版本。GitHub Pages 需 HTTPS，離線能力要先成功下載完整快取；無網路的首次造訪不能啟動。

第五階段已保存在 `backups/v2-stage5.zip`。

## 目前進度

六個主要施工階段已建構完成；接下來依使用者實測回饋進行整體優化。麻瓜專屬特效依指示暫緩，不列為已完成。沒有執行 GitHub Pages 發布或代替使用者做實機驗收。
