# V2 第一階段：架構與抽選

> 本文件為第一階段交付紀錄。第二階段已移除下述長按／放手失敗轉接層，目前操作請見 `STAGE2.md`。

## 本次範圍

- `index_v1_backup.html` 為原版完整備份，未修改。
- `js/config.js` 集中管理機率、階級範圍、評語與主要演出時間。
- `js/result-system.js` 管理獨立抽選與 Session。每次「開始鑑定」或「重新鑑定」只建立一次不可變結果。
- 麻瓜預設啟用、8%，先抽麻瓜；未中才抽啟用屬性、階級、魔力值。光暗仍限定 A / S / SS = 60 / 30 / 10%。
- 普通 E 為 10–199，麻瓜為 1–9、無屬性。SS 的內部數值落在 1000–9999，畫面仍用 9999 暴衝與 `????` 表示無法測定。
- 全關屬性不會自動把麻瓜機率提高。只有啟用麻瓜且設為 100% 時可單獨鑑定麻瓜。
- 設定在本次開啟期間保留；重新載入回到預設，沿用原版行為。

## 狀態與後續接點

SETTINGS → STANDBY → INTERACTION → RESONANCE_COMPLETE → GATHER → ELEMENT_REVEAL → MANA_SCAN → RANK_CONFIRM → RESULT

目前舊渲染 PHASE 作為展示轉接層：長按、放手失敗、固定核心與舊特效仍保留。共鳴完成目前直接接到收束，屬性獨立一幕、麻瓜專屬動畫與自由滑動依序於後續階段實作。放手失敗回待機不重抽該輪結果。

重置統一清除序列計時器、跑分 RAF、特效 RAF、pointer capture、階級動畫與結果狀態。原有兩個常駐視覺 RAF 不重複建立；後續粒子重構再合併為單一迴圈。

## 程式檢查

開發檢查指令：`node tests/stage1.cjs`。Node 僅供開發測試，網站執行不需要 Node、npm 或建置。

測試涵蓋機率區間邊界、六屬性單獨開啟、所有階級數值上下界、麻瓜獨立抽選、不合法設定、狀態轉移、實際頁面腳本的麻瓜／火／光／暗／SS 流程、放手重試、多 pointer、結果停留、重新鑑定、回設定、計時器與 RAF 數量，以及快取資源存在性。DOM/Canvas 使用測試替身，不代表實機視覺或 PWA 已驗證。

## 交給使用者的實機檢查

1. 關閉麻瓜，只開火：依目前提示按住完成，確認火屬性、階級、數值；E 不再出現麻瓜評語。
2. 全部屬性關閉，麻瓜機率 8%：開始按鈕應停用。啟用麻瓜並改成 100%：應可開始，結果為「無屬性／麻瓜／1–9」。
3. 只開光，再只開暗，關閉麻瓜：各自特效正常，最終階級只會是 A、S 或 SS。
4. 在結果頁等待、打開小型工作人員選單、重新鑑定，再回設定。結果應持續停留，設定仍在，沒有上一輪階級彈跳或跑分殘留。
5. 如抽到 SS：確認暴衝後停在 `????` 與 SS。程式測試已有固定抽選覆蓋，無需反覆抽到才算完成本階段測試。

## PWA / GitHub Pages

所有網址均使用相對路徑。部署時需一起上傳 index、js、service-worker、manifest 與兩張既有 icon；不需上傳 tests 與備份。manifest 沒有新增資源需求，保留原設定。

快取版本已改為 `mana-pwa-v2-stage1-1`，新增兩個 JS。新 worker 全部資源下載成功後才安裝，等待舊頁面關閉後啟用，避免強制接管進行中的鑑定。部署後先連網開啟取得更新，關閉同站 Safari 分頁及 PWA 再開啟；更新完成後再測離線。未啟用的新版本不會只靠當前頁面重新整理強制接管。未來任何快取檔案更新需再遞增版本。

第一階段完成後停止，等待使用者測試回饋；不自動開始第二階段。
