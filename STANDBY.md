# iPhone PWA 待機細節調整

依提供的 iPhone 13 截圖與待機規格調整：

- 中央改為低亮度半透明水體：不同相位的表面波讓輪廓緩慢起伏，側邊保留淡反光，內部柔霧慢速流動；使用一般透明疊合避免中央累積白光。低畫質保留水體。
- 真實粒子分成慢公轉、局部環流、懸浮及上下漂移；保留手指力場、共鳴與結果抽選。
- 五種低頻事件錯開：單顆光點吸入、三顆細塵緩慢外移、內部淡流、雲核脈動、小範圍短渦流。沿用已有粒子，不加事件計時器或新粒子物件。
- 待機亮星大幅降低存在感，只保留少量較清晰菱形星芒；不同相位緩慢明暗變化。前景光斑淡化，細流保持低透明度。
- 觸碰後保留相同物件、位置與速度，約半秒漸變回互動外觀；待機事件不改變共鳴值或結果。
- 提示改為固定兩行「請觸碰魔力／試著自由滑動」，降低字重、採 7 秒微弱亮度呼吸。
- HTML 根背景、PWA 啟動底色與 theme-color 改為深靛紫；客人動畫背景向安全區延伸，避免視窗外露出淺色底。設定與結果仍使用原配色。

## 驗證與限制

`node tests/standby.cjs` 檢查兩分鐘待機中五種事件、間隔、穩定粒子物件池、不同局部軌跡、觸碰位置連續性及 LOW 畫質仍有霧核與細流。其餘階段與設定回歸檢查照常執行。

未開啟瀏覽器或代替使用者實機驗收。截圖不足以確認 iOS 底部淺色帶的全部原因；已修正可見的淺色根背景及安全區露底風險，需更新後觀察。

## iPhone 建議確認

1. 待機看 30–45 秒：觀察霧核局部呼吸、吸入／吐出、淡流、微渦流是否足夠細微。
2. 提示維持兩行，底部及安全區不再露出淺色帶。
3. 手指在不同位置觸碰、拖動，魔力平順回應，沒有重新排列或突然跳位。
4. 完成一輪、重新鑑定：待機事件與呼吸從初始狀態重新開始，設定仍保留。

快取版本 `mana-pwa-v2-standby-2`。部署 index、所有 JS、manifest 與 service-worker；連網檢查更新後關閉同站分頁與 PWA 再開啟。舊版備份為 `backups/before-standby-nebula.zip`。麻瓜特效仍暫緩。

## PC 待機失控修正

待機混合值缺少上限，隨等待時間持續增加，造成粒子目標外插與無效透明度。現限制在 0～1，新增兩分鐘待機及長時間等待後觸碰退場的回歸測試，並檢查 Canvas 透明度範圍。已通過程式檢查；水體動態仍待使用者 PC／PWA 實測。
